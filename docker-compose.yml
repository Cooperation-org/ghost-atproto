version: '3.8'

services:
  ghost-bluesky-bridge:
    build: .
    ports:
      - "3000:3000"
    environment:
      - BLUESKY_HANDLE=${BLUESKY_HANDLE}
      - BLUESKY_PASSWORD=${BLUESKY_PASSWORD}
      - GHOST_WEBHOOK_SECRET=${GHOST_WEBHOOK_SECRET}
    restart: unless-stopped